{
  "name": "joycon2.js",
  "version": "0.1.0",
  "main": "./dist/joycon2.mjs",
  "module": "./dist/joycon2.mjs",
  "types": "./dist/joycon2.d.ts",
  "exports": {
    ".": {
      "import": "./dist/joycon2.mjs"
    }
  },
  "scripts": {
    "build": "vite build --config vite.config.ts && vite build --config vite.config.ts",
    "build:types": "tsc -p tsconfig.json",
    "build:with-types": "npm run build && npm run build:types && echo 'lib build complete'",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "vite": "latest",
    "typescript": "latest",
    "eslint": "latest",
    "prettier": "latest"
  }
}
