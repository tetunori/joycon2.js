// Bridge moved into the library package: lib/src/bridge.ts
// Build the lib (`cd lib && npm run build`) to generate `dist/bridge.umd.js` and load that
// from the example. The previous local bridge file is kept as a stub for compatibility.
console.warn('lib-bridge.js is deprecated — use ../dist/bridge.umd.js (built from lib/src/bridge.ts)');
